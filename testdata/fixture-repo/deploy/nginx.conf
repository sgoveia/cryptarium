# Example nginx TLS config for fixture scans.
# Comment must not produce findings:
# ssl_ciphers TLS_RSA_WITH_AES_128_CBC_SHA;

server {
    listen 443 ssl;
    ssl_ciphers TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256;
}
